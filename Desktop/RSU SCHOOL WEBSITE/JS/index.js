function showMessage(text) {
    
    
    if (text === undefined) {
        text = 'empty message';
    } else {
        text = 'a test';
    }
    alert(text);
    
}
// ignore this line of code cos it is a comment
showMessage('boy');